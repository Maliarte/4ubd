  CREATE TABLE LOGIN (	
        ID_LOG INT NOT NULL,
	USUARIO" VARCHAR(15) NOT NULL , 
	SENHA VARCHAR(15) NOT NULL, 
	ACESSO INT NOT NULL,
	FUNCIONARIO_ID_FUNC INT NOT NULL E, 
	CONSTRAINT "LOGIN_PK" PRIMARY KEY ("ID_LOG")
  USING INDEX  ENABLE
   ) ;
