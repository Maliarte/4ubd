  CREATE TABLE "FORNECEDOR" (	
        ID_FOR INT NOT NULL, 
	CIDADE VARCHAR2(45) NOT NULL, 
	TELEFONE NUMBER(20,0) NOT NULL, 
	NOME VARCHAR2(45) NOT NULL, 
	CNPJ VARCHAR2(45) NOT NULL, 
	SETOR VARCHAR2(45) NOT NULL, 
	ESTADO VARCHAR2(2) NOT NULL, 
	RUA VARCHAR2(45) NOT NULL, 
	NUMERO" VARCHAR2(45) NOT NULL, 
	CONSTRAINT "FORNECEDOR_PK" PRIMARY KEY ("ID_FOR")
  USING INDEX  ENABLE
   ) ;
