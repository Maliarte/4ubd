  CREATE TABLE FUNCIONARIO (	
        ID_FUNC INT NOT NULL, 
	CPF INT NOT NULL, 
	RUA VARCHAR(45), 
	SETOR VARCHAR(45), 
	NOME VARCHAR(45) NOT NULL, 
	NUMERO INT, 
	ESTADO VARCHAR(2), 
	CIDADE VARCHAR(45), 
	TELEFONE INT, 
	DATA_NASC DATE, 
	CARGO VARCHAR(45), 
	SALARIO FLOAT(126), 
	ENTRADA_SERV TIMESTAMP (6) TIME, 
	SAIDA_SERV TIMESTAMP (6) TIME, 
	CONSTRAINT "FUNCIONARIO_PK" PRIMARY KEY ("ID_FUNC")
  USING INDEX  ENABLE
   ) ;
