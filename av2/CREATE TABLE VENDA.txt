  CREATE TABLE VENDA (	
	ID_VEN INT AUTO_INCREMENT, 
	VALOR FLOAT(126) NOT NULL ENABLE, 
	DATA DATE NOT NULL ENABLE, 
	DESCRICAO VARCHAR(250) NOT NULL ENABLE, 
	FORMA_PAGAMENTO VARCHAR(10) NOT NULL ENABLE, 
	FUNCIONARIO_ID_FUNC INT NOT NULL ENABLE, 
	CLIENTE_ID_CLI INT NOT NULL ENABLE, 
	CONSTRAINT "VENDA_PK" PRIMARY KEY ("ID_VEN")
  USING INDEX  ENABLE
   ) ;
