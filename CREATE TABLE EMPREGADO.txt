  CREATE TABLE EMPREGADO(	
	NOME CHAR(1), 
	RG  INT NOT NULL, 
	CIC INT, 
	DEPTO INT, 
	RG_SUPERVISOR INT, 
	SALARIO FLOAT(126), 
	CONSTRAINT "EMPREGADO_PK" PRIMARY KEY ("RG")
  USING INDEX  ENABLE
   ) ;
