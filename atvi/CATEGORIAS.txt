 CREATE TABLE CATEGORIAS (	
	ID INT NOT NULL, 
	CATEGORIA VARCHAR(50), 
	CONSTRAINT "CATEGORIAS_PK" PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   ) ;

   COMMENT ON COLUMN "CATEGORIAS"."ID" IS 'chave primária';
   COMMENT ON COLUMN "CATEGORIAS"."CATEGORIA" IS 'nome categoria';
